<% provide :title, t("views.layouts.users") %>

<h1><%= t "views.users.index.all_users" %></h1>
<%= will_paginate %>
<ul class="users">
  <% @users.each do |user| %>
    <li>
      <%= user.name %>
    </li>
    <br>
  <% end %>
</ul>
<%= will_paginate %>
